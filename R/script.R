

#set.seed(556)
#sigma <- 1
#beta <- 0.1 * pca$rotation[, 1] + rnorm(p, 0, 0.01)



# CASE 1: no influential points -----------------------------------------------------------------------------------

# Simulate outcome Y.
#y <- X %*% beta + rnorm(n, 0, sigma)
